name: VertexEngine Build

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4
      with:
        submodules: recursive

    # Using MSYS2 is the industry standard for MinGW on GitHub Actions
    - name: Setup MSYS2
      uses: msys2/setup-msys2@v2
      with:
        msystem: MINGW64
        update: true
        install: >-
          mingw-w64-x86_64-gcc
          mingw-w64-x86_64-cmake
          mingw-w64-x86_64-ninja

    - name: Configure CMake
      # Use 'shell: msys2 {0}' to ensure we are using the MSYS2 environment
      shell: msys2 {0}
      run: |
        cmake -S . -B build -G Ninja `
        -DCMAKE_BUILD_TYPE=Release

    - name: Build Engine
      shell: msys2 {0}
      run: cmake --build build --config Release

    - name: Upload Build Artifact
      uses: actions/upload-artifact@v4
      with:
        name: VertexEngine-Windows
        path: build/VertexEngine.exe
